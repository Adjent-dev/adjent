# Compiled binary, built by `go build -o adjent .`
/adjent

# Test and coverage output
*.out
coverage.txt

# Editor and OS noise
.DS_Store
.idea/
.vscode/

# Signing keys and recorded logs must never be committed
*.key
*.pub
*.pem
adjent.log
